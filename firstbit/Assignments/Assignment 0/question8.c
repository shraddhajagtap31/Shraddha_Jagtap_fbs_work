#include <stdio.h>

void main(){
	int length,breadth,perimeter;
	length = 100;
	breadth = 200;
	perimeter = length * breadth;
	printf("The perimeter of the rectangle having length as %d and breadth as  %d is %d", length,breadth,perimeter);
}