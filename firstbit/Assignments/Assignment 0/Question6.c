#include <stdio.h>

void main(){
	int number,square,cube;
	number = 10;
	square = number * number;
	cube = number * number * number;
	printf("The square of %d is %d and the cube is %d", number,square,cube);
}